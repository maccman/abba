@import './mixins'

section.experiments {
  hbox()

  .list {
    box-flex: 1
    margin-right: 0px
  }

  .new {
    width: 420px
  }

  pre {
    margin: 0
  }

  table {
    .index {
      width: 20px
    }
  }
}
